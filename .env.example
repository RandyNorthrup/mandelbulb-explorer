# Mandelbulb Explorer is a static client-only app.
# There are no required environment variables and no runtime secrets.
#
# Optional Vite public-base override if you ever stop using the relative
# `base: './'` in vite.config.ts:
# VITE_BASE=./
